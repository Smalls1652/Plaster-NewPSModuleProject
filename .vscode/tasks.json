{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build PowerShell Module",
            "type": "shell",
            "command": ".\\build.ps1",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}